"use client";

type Props = {
  input: string;
  onLoadSummary: (summary: string) => void;
};

const LoadSummary = () => {
  //const {} = useSWR(`/api/summary/${articleId}`, )

  return <></>;
};

export default LoadSummary;
